import { css } from "@emotion/react";

export const onWideScreen = "@media (min-width: 769px)";

export const resetButton = css`
  cursor: pointer;
  box-sizing: border-box;

  border: 0;
  padding: 0;
  color: inherit;
  background-color: transparent;

  font-family: inherit;
  font-size: inherit;
`;

export const resetLink = css`
  text-decoration: none;

  color: inherit;

  font-family: inherit;
  font-size: inherit;
`;

export const resetInput = css`
  font-family: inherit;
  font-size: inherit;
  color: inherit;
  background-color: transparent;
`;
